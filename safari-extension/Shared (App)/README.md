# Shared App Code & Resources

This folder represents shared codebase models and controllers for Multiplatform (macOS + iOS) wrapping if compiling the application for both platforms in Xcode.

Place any shared resources (such as Apple App Groups, shared settings, or local network endpoints) within this workspace when creating the Xcode project.
